import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-laravel-image',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
